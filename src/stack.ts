export function balanceBrackets(str: string){
  const stack: string[] = [];
  const openInBrackets = ['([{'];
  const closeInBrackets = [')]}'];
  const brackersPairs = {
    '(': ')',
    '[': ']',
    '{': '}'    
  }

}